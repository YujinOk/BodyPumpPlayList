import { search, lookup } from "https://cdn.skypack.dev/itunes-store-api";
import { search } from "itunes-store-api";
const { results } = await search("M83");
// 1. randomise the playlist
//  from different release no. change equivelent tracks alike
// 2. be able to record what has played in that particular class

// end point to trigger the function
